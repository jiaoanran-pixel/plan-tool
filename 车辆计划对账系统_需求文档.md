# 车辆计划统计与对账系统 — 需求文档

版本：v0.2（需求已确认）  
日期：2026-08-12  
状态：开发完成，初版已交付

---

## 1. 项目概述

建设一套**手机端 + 电脑端**均可使用的车辆计划管理与对账工具，用于：

- 把每天新增的运输计划以**卡片**形式登记、归集；
- 支持**手动录入**、**剪贴板粘贴自动识别**、**单据图片 OCR 自动匹配**三种录入方式；
- 对每一车计划归集对应的**装车磅单、卸车磅单、运单**图片，形成完整资料；
- 支持**每日/每月对账登记**：按日期查看计划清单、资料完整度与统计汇总；
- 一键导出符合对账要求的 **Excel 附件**（列结构见第 7 节，依据用户提供的截图整理）。

---

## 1.1 需求确认结论（v0.2）

以下为已确认的需求口径：

| 问题 | 确认结果 |
| --- | --- |
| 字段 | 卡片加入"计划到站日期"；图片位为装车磅单、卸车磅单、运单三个 |
| 多图 | 每种单据每个计划只传 **1 张** |
| 使用范围 | 单人使用；本地存储 |
| 手机访问 | 浏览器访问；手机在外网访问为后期优化项（内网穿透/云部署） |
| 对账统计 | 主要按**车数**统计，另含单据齐全数、缺单数、金额合计 |
| 价格 | 价格为**单价（元/吨）**；金额 = 价格 × 净重，净重从卸车磅单 OCR 提取 |
| Excel | 磅单/运单图片**嵌入 Excel**；列结构按截图，另加净重、金额、备注列 |
| 剪贴板 | 来源为微信；已按用户提供的真实样本文本定制解析规则 |

---

## 2. 用户与使用场景

| 场景 | 说明 |
| --- | --- |
| 计划录入 | 调度/登记人每天收到新的运输计划，用手机或电脑快速录入 |
| 单据归集 | 每车完成装卸后，把磅单、运单拍照上传，自动挂到对应计划卡片 |
| 每日对账 | 每天核对当日计划的单据是否齐全、车数/金额是否对得上 |
| 每月对账 | 月末按月度汇总导出 Excel，交给财务/上游核对 |

---

## 3. 核心对象：计划卡片

每个运输计划是一张卡片，字段如下：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 装车日期 | 日期 | 是 | 计划装车日期，参与 OCR 匹配 |
| 车头号（车号） | 文本 | 是 | 如：冀J52552，参与 OCR 匹配 |
| 气源地 | 文本 | 否 | 如：正安 |
| 供应商 | 文本 | 否 | 如：禾兴 |
| 站点 | 文本 | 否 | 如：宜章西西站 |
| 计划到站日期 | 日期 | 否 | 已确认纳入卡片；日期类型（粘贴文本中的"13号19点"自动转成日期，时刻记入备注） |
| 价格 | 数字 | 否 | 单价还是每车总价待确认（问题 6） |
| 装车磅单 | 图片 | 否 | 已确认每种 1 张 |
| 卸车磅单 | 图片 | 否 | 已确认每种 1 张；OCR 自动提取净重 |
| 运单 | 图片 | 否 | 已确认每种 1 张 |
| 备注 | 文本 | 否 | 额外说明（建议新增） |
| 挂车号/司机/承运公司 | 文本 | 否 | 剪贴板识别自动带出，展开区可编辑 |

卡片支持：**新增、修改、删除**；支持按日期、车号、站点、供应商、单据完整度搜索筛选。

每张卡片提供**一键复制**按钮：把车号、日期、站点、价格、净重、金额、单据状态等关键信息复制成文本，可直接粘贴到微信同步给其他人。

---

## 4. 功能需求

### F1 计划卡片管理

- 卡片列表按日期倒序展示；支持日期范围筛选。
- 新增/编辑表单：以上字段手动录入，带格式校验（日期、价格数字）。
- 删除需二次确认；删除后其图片一并移除。
- 卡片显示"资料完整度"状态：
  - 待补充：缺少任一单据图片；
  - 已齐全：装车磅单、卸车磅单、运单均已上传（或按确认后的字段口径）。
- 重复提醒：同一车号 + 同一装车日期已存在时提示，避免重复录入。

### F2 剪贴板粘贴自动识别

- 在新增/编辑界面支持"粘贴识别"：用户把计划文字复制后粘贴，系统自动解析出字段并预填表单。
- 识别出的字段可编辑修改，确认后保存。
- 解析规则基于真实粘贴样本来定制（问题 2：需要用户提供 1~2 段实际复制的文本）。
- 识别失败或字段缺失时，仅预填能识别的部分，其余手动补录。

已按用户提供的微信文本样本适配，示例：

```text
正安-宜章西东站13号19点
12号装车
冀J0B318冀JM09D挂
余佑江
512323197805201614
17783662621
周润群
512323198201077047
13658473239
沧州市安兴特种货物运输有限公司
供应商：浙江禾兴；价格5900
```

自动识别结果：气源地=正安、站点=宜章西东站、计划到站=13号19点、装车日期=12号（按本月补全）、车头号=冀J0B318、挂车号=冀JM09D挂、供应商=浙江禾兴、价格=5900、司机=余佑江（押运周润群）、承运公司=沧州市安兴特种货物运输有限公司。

### F3 单据图片 OCR 识别与自动匹配

流程：

1. 用户拍照或从相册/文件选择一张单据图片上传（手机、电脑均可）；
2. 服务端进行 OCR，提取文本（中文为主，兼容英文/数字）；
3. 从 OCR 文本中解析关键信息：
   - **车号**：如"冀J52552"（省份简称 + 字母 + 数字）；
   - **装车日期**：如"2026-08-13"、"8月13日"；
   - **单据类型**：通过关键词判断是装车磅单、卸车磅单还是运单；
   - **净重**：从卸车磅单提取净重，金额 = 价格 × 净重 自动计算；
4. **自动匹配**：按「装车日期 + 车号」在已有计划中查找；
   - 唯一匹配 → 自动把图片填入该计划的对应单据位置；
   - 多个匹配 → 弹出候选列表让用户选择；
   - 无匹配 → 提示"未找到对应计划"，可手动关联，或基于识别信息快速新建计划；
5. **识别失败处理**：
   - OCR 无法提取车号/日期，或类型判断不确定 → 提示"自动识别失败"，原图保留，用户可手动选择单据类型并关联到计划；
   - 匹配失败与识别失败分别提示，避免混淆。

### F4 每日/每月对账登记

- **每日对账**：选择日期，展示该日全部计划卡片与汇总：
  - 车数、单据齐全车数、缺单车辆数；
  - 价格合计（金额口径待确认，见问题 6）；
  - 未上传单据的计划高亮提示。
- **每月对账**：选择月份，展示月度汇总：
  - 按日列出车数、齐全数、缺单数与金额；
  - 按供应商、站点、气源地小计（是否必需待确认，见问题 5）；
  - 月度合计行。
- 统计口径已确认：按**车数**统计，另含单据齐全数、缺单数、金额合计。
- 对账结果可一键导出 Excel（F5）。

### F5 Excel 导出

- 导出范围：单日 / 单月 / 自定义日期区间 / 全部。
- 列结构按截图模板：
  - 装车日期、车号、气源地、供应商、站点、计划到站日期、价格、装车磅单、卸车磅单、运单
  - 已确认额外加：净重(吨)、金额(元)、备注（按截图原列顺序排布）。
- 图片处理两种方案（问题 7 待确认）：
  - 已确认采用方案 A：磅单/运单图片**直接嵌入 Excel**（单元格显示缩略图）。
  - 方案 B：图片存入导出文件夹，Excel 内放文件名/超链接（文件小，但图与表分离）。
- 底部自动生成**合计行**（车数、金额合计）。
- 可选"汇总 Sheet"：按供应商/站点/气源地汇总。

### F6 数据管理

- 数据保存在本地 SQLite（或 JSON），无需联网。
- 支持一键备份/恢复（导出全部数据 JSON）。
- 删除单据/计划可恢复吗？——一期不做回收站，删除前二次确认。

---

## 5. 界面与交互（简要）

- **整体**：响应式 Web 页面，手机浏览器与电脑浏览器同样可用；手机端卡片单列，电脑端网格/列表自适应。
- **主界面**：顶部日期筛选（今天/本月/自定义）+ 汇总数字；下方为计划卡片流；右上角"新增计划"。
- **新增/编辑**：表单页；包含"粘贴识别"按钮与"上传图片"区（拍照/相册/文件）。
- **图片上传**：上传后显示识别中状态；成功自动填入并提示"已自动关联到 XX 计划"；失败显示"自动识别失败，请手动选择"。
- **对账页**：日/月切换，展示统计表与清单，提供"导出 Excel"按钮。

---

## 6. 技术方案建议

| 层 | 方案 | 说明 |
| --- | --- | --- |
| 服务端 | 本地 Python 服务（沿用之前"小工具"的 server.py 风格） | 数据与图片统一存本地，手机通过局域网访问 |
| 数据库 | SQLite | 单机、可靠、零配置 |
| 前端 | 响应式 HTML/JS 单页 | 手机/电脑共用一套页面，无需安装 App |
| OCR | macOS 自带 Vision 框架（Objective-C 辅助程序） | 中文识别效果好、免费、离线；由 Python 服务调用 |
| Excel 生成 | openpyxl | 生成 .xlsx，支持嵌入图片 |
| 手机访问 | 电脑与手机连同一 Wi-Fi，手机浏览器打开电脑局域网地址（可生成二维码扫码） | 无需上架应用商店 |

> 备选：若需要手机在**外网/无局域网**时也能用，需要把服务部署到公网或云服务器（涉及服务器费用、域名等），见问题 4。

---

## 7. Excel 模板结构（依据截图 OCR 识别）

从用户截图识别出的表头：

| A | B | C | D | E | F | G | H | I | J |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 气源地 | 装车日期 | 车号 | 供应商 | 站点 | 计划到站日期 | 价格 | 装车磅单 | 卸车磅单 | 运单 |

示例数据行（OCR 结果，可能有误差）：  
正安 | 8月13日5点 | 冀J52552 | 禾兴 | 宜章西西站 | … | 5900 | … | … | …

**注意**：
- 你文字里"卸车磅单（上传图片）"出现两次，但截图显示是**装车磅单、卸车磅单、运单**三个图片列——需求按三个图片位处理（待确认）。
- 截图中有**计划到站日期**列，但你列的字段里没有——是否需要也作为卡片字段待确认。

---

## 8. 可实现性评估

### 能实现（一期范围）

1. 手机 + 电脑浏览器均可使用的计划卡片管理（增删改查、筛选、状态标记）；
2. 剪贴板粘贴文本自动识别字段（需提供真实样本定制规则）；
3. 单据图片 OCR 提取文字，按"装车日期 + 车号"自动匹配并填入对应图片位；
4. 单据类型（装车磅单/卸车磅单/运单）关键词自动分类，失败时手动选择；
5. 每日/每月对账视图与统计（车数、金额合计、缺单提醒）；
6. Excel 导出（按截图列结构，支持合计行、嵌入图片或超链接）；
7. 本地数据存储与备份。

### 有条件实现 / 受限

1. **OCR 准确率**：受拍照质量影响（模糊、倾斜、反光、手写字），识别结果需人工核对；对常见印刷体磅单效果较好；
2. **类型自动分类**：基于关键词规则的准确率在多数场景较高，但不是 100%，失败时需要手动选类型；
3. **剪贴板识别**：只能识别已适配的文本格式；不同来源（物流平台、Excel、微信）格式不同，需逐个加规则；
4. **手机访问**：默认手机与电脑在同一局域网可用；在外网使用需额外部署（云服务器/内网穿透）；
5. **匹配成功前提**：磅单图片上必须能识别出车号和日期；缺任一信息则无法自动匹配。

### 一期不能实现

1. **自动接收微信/钉钉/短信中的图片并归集**：微信个人号没有开放接口，无法自动读取聊天里的图片，只能手动保存后上传；
2. **完全无人值守的自动对账**：识别与匹配存在误差，必须保留人工确认环节；
3. **手机原生 App**：一期为网页版；如要上架 App Store/应用市场需另行开发 iOS/Android 客户端；
4. **对不同磅单模板的 100% 兼容**：各地磅单样式差异大，需要根据实际单据持续调优。

---

## 9. 待确认问题清单

> v0.2 说明：以下问题已全部获得用户答复，保留备查。

### 必答（影响核心设计）

1. ✅ **字段确认**：加入"计划到站日期"；图片位为装车磅单 + 卸车磅单 + 运单三个。
2. ✅ **粘贴样本**：已提供微信样本，解析规则已按该格式实现。
3. ✅ **使用范围**：单人使用；本地存储；手机外网访问为后期优化项。
4. ✅ **手机访问方式**：浏览器访问（同一局域网）。
5. ✅ **对账统计口径**：按车数统计，另含齐全/缺单/金额。
6. ✅ **价格含义**：单价（元/吨）；金额 = 价格 × 净重，净重由卸车磅单 OCR 提取。
7. ✅ **Excel 图片**：图片嵌入 Excel。
8. ✅ **多图**：每种单据 1 张。

### 可选（优化项）

9. 是否需要"批量粘贴多条计划"一次生成多张卡片？
10. 是否需要 Excel 导入（把已有表格直接导入系统）？
11. 已对账月份是否锁定（不允许修改/删除），防止误改历史数据？
12. 是否需要按天生成"对账日报"并自动归档？

---

## 10. 建议的优化项（可加分项，按需纳入）

- **状态看板**：卡片显示"资料完整度"，缺哪个单据一目了然；
- **重复计划提示**：同车号 + 同装车日期自动提醒；
- **OCR 增值提取**：从磅单自动提取毛重/皮重/净重，金额自动计算；
- **批量粘贴**：一次粘贴多行，批量生成计划卡片；
- **自动备份**：每天自动备份数据文件；
- **扫码打开**：电脑端显示二维码，手机扫码直接进入系统。

---

## 11. 交付计划（确认后）

1. ✅ 确认需求与答案；
2. ✅ 搭建项目结构（Python 服务 + 前端 + SQLite + OCR 辅助程序）；
3. ✅ 实现计划卡片 CRUD 与筛选；
4. ✅ 实现剪贴板识别与图片 OCR 匹配；
5. ✅ 实现对账统计与 Excel 导出；
6. ✅ 联调测试（接口全链路测试通过）。
